{
	"name": "CoderGitOpsAdmin",
	"build": {
		"dockerfile": "Dockerfile"
	},
	"remoteEnv": {
        	"CODER": "devcontainer"
        }
	// Use 'postCreateCommand' to run commands after the container is created.
	//"postCreateCommand": "curl -L https://coder.com/install.sh | sh"
}